%br/
= link_to "Add New Post", new_post_path

%br/
%h4 Listing of Posts
%table
  %thead
    %tr
      %th
        Title
      %th
        Description
      %th
        Created AT
      %th
        Days
      %th
        Action
  %tbody
    - unless @posts.blank?
      - @posts.each do |post|
        %tr 
          %td
            = post.title
          %td
            = post.description
          %td
            = post.created_at
          %td
            = post.days
          %td
            = link_to "Edit Post", edit_post_path(post)
            |
            = link_to "Delete", post, :method => :delete, :confirm => "Are you sure?"   
    - else
      No Records Found in Data Base..!     
